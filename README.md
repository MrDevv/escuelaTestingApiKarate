# Proyecto de Karate
Este es un proyecto de karate para la optimización de pruebas de la API  `PetStore`

## Prerrequisitos
* Java 17
* Apache Maven

## Comandos
Comando para ejecutar:

```mvn clean test -Dtest=UsersRunner -Dkarate.options="--tags @regresion"```